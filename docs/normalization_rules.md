# Normalization Rules

This document is generated from `tests/normalization/cases.yaml`.
Do not edit this file directly.

## Rules & Examples

### normalize_line_endings
Normalize line endings to LF and trim surrounding whitespace.

Input (escaped):
```text
Line one\r\nLine two\rLine three
```

Expected (escaped):
```text
Line one Line two Line three
```

### trim_lines
Trim whitespace at the start and end of each line.

Input (escaped):
```text
  Alpha  \n\tBeta\t\n  Gamma
```

Expected (escaped):
```text
Alpha Beta Gamma
```

### collapse_blank_lines
Collapse multiple blank lines to a single blank line.

Input (escaped):
```text
A\n\n\n\nB
```

Expected (escaped):
```text
A B
```

### preserve_single_blank_line
Preserve a single blank line between blocks.

Input (escaped):
```text
First line\n\nSecond line
```

Expected (escaped):
```text
First line Second line
```

### normalize_quotes
Normalize quotes to << >>.

Input (escaped):
```text
\u201cHello.\u201d
```

Expected (escaped):
```text
<<Hello.>>
```

### ascii_transliteration
Transliterate to ASCII (NFKD) and drop non-ASCII characters.

Input (escaped):
```text
Kl\xfcft skr\xe4ms inf\xf6r p\xe5 f\xe9d\xe9ral
```

Expected (escaped):
```text
Kluft skrams infor pa federal
```
