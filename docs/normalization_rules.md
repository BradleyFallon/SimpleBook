# Normalization Rules

This document is generated from `tests/normalization/cases.yaml`.
Do not edit this file directly.

## Rules & Examples

### normalize_whitespace
Collapse all whitespace (line breaks, tabs, repeated spaces) into single spaces.

Example 1:

Input (escaped):
```text
Line one\r\nLine two\rLine three
```

Expected (escaped):
```text
Line one Line two Line three
```

Example 2:

Input (escaped):
```text
  Alpha  \n\tBeta\t\n  Gamma
```

Expected (escaped):
```text
Alpha Beta Gamma
```

Example 3:

Input (escaped):
```text
A\n\n\n\nB
```

Expected (escaped):
```text
A B
```

### normalize_quotes
Normalize double quotes to << >> and keep content order.

Example 1:

Input (escaped):
```text
\u201cHello.\u201d
```

Expected (escaped):
```text
<<Hello.>>
```

Example 2:

Input (escaped):
```text
\u201cBut if you \ndon\u2019t\n,\u201d said Pooh, \u201cI shall have to let go, and that would spoil \nme\n.\u201d
```

Expected (escaped):
```text
<<But if you dont ,>> said Pooh, <<I shall have to let go, and that would spoil me .>>
```

### ascii_transliteration
Transliterate to ASCII (NFKD) and drop non-ASCII characters.

Example 1:

Input (escaped):
```text
Kl\xfcft skr\xe4ms inf\xf6r p\xe5 f\xe9d\xe9ral
```

Expected (escaped):
```text
Kluft skrams infor pa federal
```

Example 2:

Input (escaped):
```text
R\xe9sum\xe9\u2014na\xefve fa\xe7ade
```

Expected (escaped):
```text
Resume--naive facade
```

Example 3:

Input (escaped):
```text
Hello\u2014there
```

Expected (escaped):
```text
Hello--there
```

### format_markers
Normalize inline formatting into markers (/// for italics, ** for bold,

Example 1:

Input (escaped):
```text
This is ///italic/// text.
```

Expected (escaped):
```text
This is ///italic/// text.
```

Example 2:

Input (escaped):
```text
Make **bold** and ///italic/// together.
```

Expected (escaped):
```text
Make **bold** and ///italic/// together.
```

Example 3:

Input (escaped):
```text
## Heading \u2014 Title
```

Expected (escaped):
```text
## Heading -- Title
```
